apiVersion: v1
kind: Service
metadata:
  name: ridesphere-backend
  labels:
    app.kubernetes.io/name: ridesphere
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ridesphere
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8000
      targetPort: http
